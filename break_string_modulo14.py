

palabra = "HOLAPARCECOMOVAMOSESPEROQUEVAYASMUYBIENPORQUEYOTAMBIEN"
def break_string_modulon(string, n):
    alfabeto = [chr(x) for x in range(ord("A") , ord("Z")+1)]
    sub_strings = ["" for x in range(len(alfabeto))]
    print(sub_strings)
    index = 0
    while(index < len(string)):
        for i in range(len(palabra)):
            if(i%len(alfabeto) == )


break_string_modulon(palabra , 3)

